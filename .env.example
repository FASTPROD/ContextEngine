# ContextEngine Environment Variables
# Copy to .env and customize as needed.
# All variables are OPTIONAL — ContextEngine works zero-config out of the box.

# ──────────────────────────────────────────────────────────────────────────────
# Client (MCP Server / CLI)
# ──────────────────────────────────────────────────────────────────────────────

# Override the activation API endpoint (default: https://api.compr.ch/contextengine)
# CONTEXTENGINE_API=https://api.compr.ch/contextengine

# Path to a custom contextengine.json config file
# CONTEXTENGINE_CONFIG=/path/to/contextengine.json

# Comma-separated list of workspace root directories to scan
# CONTEXTENGINE_WORKSPACES=/Users/me/Projects,/Users/me/Work

# ──────────────────────────────────────────────────────────────────────────────
# Activation Server (server/ only — not needed for regular usage)
# ──────────────────────────────────────────────────────────────────────────────

# Port for the activation server (default: 8010)
# ACTIVATION_PORT=8010

# Path to the SQLite license database (default: ./data/licenses.db)
# DB_PATH=/var/www/contextengine-server/data/licenses.db

# Path to encrypted delta module bundles (default: ./delta-modules/)
# DELTA_DIR=/var/www/contextengine-server/delta-modules/

# Path to compiled ContextEngine dist/ for delta generation
# CONTEXTENGINE_DIST=/var/www/contextengine-dist
