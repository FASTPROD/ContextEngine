# Session 11 — 2026-06-23 (afternoon) — Phase 1c: install-autostart + Claude Code terminal capture

**Scope**: Same day as Session 10 — but afternoon focus shifted from "ship 2.1.0 + verify it works in a different project" (Session 10) to "close the adoption gap so non-technical users can actually use it". Two specific gaps closed: (1) the `nohup npx ...` server-start friction, and (2) the missing capture surface for terminal Claude Code sessions.

Continues from [SESSION_10](SESSION_10_2026-06-23.md).

## Trigger

The user did the CLI-side verification from `/Users/yan/Projects/FC_project` (a project unrelated to ContextEngine) — `npx @compr/opscontext-mcp@2.1.0` worked from there, the detector fired stuck + silent_failure on the audit log, and the chain stayed clean. Then asked the question that drove this session:

> "this is the opsC using the browser and not VS terminal? or other terminal? or it do both?"

The honest answer surfaced the gap: browser ✓, VS Code chat panel ✓, VS Code Command Palette ✓, plain Claude Code in the terminal ❌. Then:

> "This 'wire a Claude Code hook' is the main aim initially! so do it !"

So this session closes that. Then a second question surfaced the OTHER friction:

> "I need a button to do this: from a localhost url or online? or an addon in VS terminal or else? your reco? THINK it's not for teck or nerds, it's for regular users and vibe coding people!"

The honest answer: localhost-served buttons can't help (chicken-and-egg); online URLs can't reach localhost (browser sandbox). The right shape is a macOS LaunchAgent — set once, runs at every login.

Both are now shipped.

## What was done

### A. Claude Code hook — terminal-side capture closed

`~/.claude/hooks/opscontext-emit.sh` (bundled in repo at [defaults/claude-code-hook.sh](../../defaults/claude-code-hook.sh)). LOCK `[OPSCONTEXT-CC-HOOK]`. Pure bash + jq + curl. ~28 ms per invocation.

Hook events wired in `~/.claude/settings.json`:
- `UserPromptSubmit` → emits `vscode.prompt_submit` (feeds the `loop` heuristic)
- `PostToolUse` matcher `.*` → emits `vscode.tool_call` (feeds `stuck`, `silent_failure`)
- `SessionStart` → emits `vscode.session_start`

Critical discipline: **PostToolUse only, not PreToolUse**. Emitting both would double-count for the `stuck` heuristic and skew `silent_failure` counts. Documented in the LOCK.

Namespace choice: **`vscode.*` not `claude_code.*`**. The published 2.1.0 detector heuristics already read `vscode.*` events; namespacing under it lets the heuristics fire **today** without a parallel namespace migration. `payload.surface = "claude-code"` disambiguates for any caller that cares.

Transport: HTTP `POST /events`, NOT direct file writes. Keeps event writes serialized through the running MCP server's single in-process chain cache — sidesteps the historic concurrent-write race we found earlier today.

Safety: **silent on every failure**. Server down, secret missing, jq error → exit 0, never block Claude Code. 1 s hard timeout on the HTTP call.

E2E verified: 5 simulated PostToolUse calls → audit log → `watch --once` fired **stuck** (WARN) + **silent_failure** (CRIT), exit code 2.

### B. macOS LaunchAgent — install-autostart

`~/Library/LaunchAgents/com.opscontext.mcp.plist`. Per-user (`gui/$UID`), no root. RunAtLoad + KeepAlive + ThrottleInterval 10. Logs to `~/.contextengine/logs/mcp-{stdout,stderr}.log`.

Boot flow: `launchctl bootout` (idempotent) → `launchctl bootstrap gui/$UID <plist>`. Wait up to 30 s for port 7842 to bind, then `curl /health` to confirm.

For the user's local environment, plist points at the dev tree (`/Users/yan/Projects/ContextEngine/dist/index.js`) since he's the dev. For other users, the new `install-autostart` CLI auto-detects either a global npm install (via `npm root -g`) or a co-located `dist/`.

### C. New CLI surface (2.1.1)

Source files:
- [src/install-autostart.ts](../../src/install-autostart.ts) — LOCK `[AUTOSTART-INSTALL]`. Three commands: `install-autostart`, `uninstall-autostart`, `autostart-status`.
- [src/install-claude-hook.ts](../../src/install-claude-hook.ts) — LOCK `[CLAUDE-HOOK-INSTALL]`. Two commands: `install-claude-hook`, `uninstall-claude-hook`. Settings.json edits are **idempotent + preserve every existing hook entry** (backed up before any change).

Five new bin subcommands wired in [src/cli.ts](../../src/cli.ts) via dynamic `import("./install-*.js")` (keeps cli.ts patch small and main-loop startup fast — modules only load when their subcommand is invoked).

Help text added under the main help block.

### D. Chrome extension placeholder fix

[chrome-extension/src/options/options.html](../../chrome-extension/src/options/options.html) — Secret field placeholder said `32 hex chars` but the actual secret is 64 hex (256-bit, what `init-extension-secret` generates). Updated to `64 hex chars`. Placeholder text only — no validation change, no backwards-compat issue.

### E. Adoption-flow user-facing copy

The new help text and CHANGELOG explicitly frame **before / after** so non-technical users see the win:
- Before: `nohup npx -y --package=@compr/opscontext-mcp@2.1.0 -- opscontext > /tmp/opscontext-mcp.log 2>&1 < /dev/null &` every reboot + hand-edited `settings.json` for hooks.
- After: `opscontext install-autostart && opscontext install-claude-hook`. Two commands. Ever.

## What's live (as of 2026-06-23 ~12:00 local)

```
npm:                     @compr/opscontext-mcp@2.1.0          ← published; 2.1.1 staged locally
Marketplace:             css-llc.contextengine 0.9.0           ← live
LaunchAgent:             com.opscontext.mcp.plist              ← loaded, port 7842 listening
~/.claude/hooks/         opscontext-emit.sh                    ← installed, chmod +x
~/.claude/settings.json  3 hook entries spliced (idempotent)   ← UserPromptSubmit, PostToolUse, SessionStart
Audit log:               ~/.contextengine/audit.log            ← 9700+ records, hash chain clean post-flag-day
Tests:                   196 / 196 passing
```

## What's pending

1. **User runs through the Day-1 plan + browser capture test** — current task.
2. **npm publish 2.1.1** — staged locally; publish when the live install has been confirmed to work end-to-end.
3. **Marketplace vscode-ext 0.10** — tool-result exit codes, `Stop` event support. Phase 3.1 territory.
4. **Linux support for `install-autostart`** — write a systemd `--user` unit equivalent. ~30 min.
5. **Web Store submission** — still needs icons + screenshots + privacy policy URL.

## Findings worth remembering

### 🟡 Audit-log concurrent-write race (filed but historic)
Earlier today, `audit-verify` flagged the global log as BROKEN at index 2826. Investigation: 8 chain breaks total, all clustered on **2026-06-10 / 06-11**, all `system` actor, all `activation.legacy_signature` ↔ `session.save` racing each other on the same millisecond. The activation server (writing `legacy_signature` events at boot) was concurrent with the main MCP server (writing `session.save`); both read `prev_hash` before either wrote, then both wrote with stale data. **Zero new breaks since flag-day (2026-06-11)** because `legacy_signature` events stopped after Ed25519 cutover. The race window still exists in principle between any two concurrent writers — but with HTTP-only writes through the launchd-managed MCP server, all events go through ONE in-process chain cache, sidestepping it. Real fix: `flock` on append in `src/audit.ts`. Filed as `audit-001-write-race` for a future session.

### 🟢 28 ms hook latency is acceptable
Measured: 10 sequential PostToolUse invocations = 281 ms total (~28 ms each). Bash + jq + curl is the right toolchain for this; Node startup (~80 ms) would have added 50 ms+ per tool call and become user-visible.

### 🟢 LaunchAgent boot pattern matches the user's existing ecosystem
`com.invocme.backup-{nightly,weekly}.plist` already in `~/Library/LaunchAgents/`. Using the same idiom keeps mental model consistent.

## Open questions

1. **Should `install-claude-hook` also re-namespace events under `claude_code.*` when 2.2.0 lands?** Pros: cleaner attribution. Cons: requires bumping the http-server.ts allowlist + bumping the detector to read both `vscode.*` and `claude_code.*` for transition. **Recommendation: defer.** The `payload.surface = "claude-code"` field is enough disambiguation for now; revisit if/when the heuristics need source-specific tuning.

2. **Should the LaunchAgent restart on `npm install -g @compr/opscontext-mcp@latest`?** Currently the plist pins an absolute path so an upgrade requires re-running `install-autostart --force`. Could add a post-install npm script that detects + refreshes the plist. **Recommendation: defer.** First confirm `install-autostart` works for real users; auto-refresh is a polish step.

3. **Menu-bar app for visual liveness signal?** The user persona ("vibe coders") would benefit from a tiny menu-bar indicator showing green/amber/red based on detector signals. ~1 week of Swift work. **Recommendation: validate Phase 1 usage first.** No point building UI for a feature nobody uses yet.

## Reproduction commands

```bash
cd /Users/yan/Projects/ContextEngine

# Verify 2.1.1 is staged locally
node -p "require('./package.json').version"
# expect: 2.1.1

# Re-install autostart from the dev tree (idempotent, requires --force if already installed)
node dist/cli.js install-autostart --force

# Verify
node dist/cli.js autostart-status

# Re-install Claude Code hook (idempotent; backs up settings.json)
node dist/cli.js install-claude-hook

# Sanity: emit one event, verify it lands
echo '{"prompt":"session 11 verify","session_id":"verify","cwd":"/tmp"}' \
  | ~/.claude/hooks/opscontext-emit.sh UserPromptSubmit
tail -1 ~/.contextengine/audit.log | python3 -m json.tool

# Watch live alerts
node dist/cli.js watch --window 600
```

## Files future-me should read first

1. **[CHANGELOG.md](../../CHANGELOG.md)** § 2.1.1 — the canonical summary.
2. **[src/install-autostart.ts](../../src/install-autostart.ts)** — LaunchAgent install logic + LOCK comments.
3. **[src/install-claude-hook.ts](../../src/install-claude-hook.ts)** — settings.json splice + idempotency logic.
4. **[defaults/claude-code-hook.sh](../../defaults/claude-code-hook.sh)** — the hook script itself; this is the bash that runs on every Claude Code event.
5. **[SESSION_10](SESSION_10_2026-06-23.md)** — what shipped this morning (2.1.0 npm + 0.9.0 marketplace).
6. **[NIGHTLY.md](../../NIGHTLY.md)** — overnight 2026-06-22 → 23 wrap (Phase 2 + 3 scaffold).
