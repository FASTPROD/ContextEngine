# Session 24, 2026-09-05: deploy preflight, fleet wiring

Short by design: the full record is `docs/handover-deploy-preflight-fleet.md` (analysis by the
invoc merge session, completion table by this one). Nothing here restates it.

| Item | State |
|---|---|
| Distribution | `scripts/sync-deploy-preflight.sh`, LOCK [PREFLIGHT-ONE-SOURCE-COPIED-OUT], 6 copies current and committed |
| Wiring | 7 fleet deploy scripts, 3 shapes (files, tree, pushed), each committed in its repo, commits in the handover table |
| Verification | each script: real modified tracked file refused before build or transfer, then a clean pass after commit; ssh/scp/rsync/npm shimmed, zero server contact |
| Lesson | ONE CE learning, `mto8s97bwjfq` (deployment), written by the invoc merge session. This session's own copy `mto90kl02ktt` was a duplicate 7 minutes younger and was deleted; its extra detail (placement above the build line, sync, override, shim verification) lives in the handover |
| Pointers | global `~/.claude/CLAUDE.md` § Multi-Tenant VPS Deploy, this repo's SKILLS.md § Deploy Automation |
| Repo | `62fff06` then this doc; tree clean |

Trap worth one line: a failing `ssh` shim killed PLANK's script at its remote md5 step under
`pipefail`, before the guard. Exit code 1 looked like a refusal and was not. The shim had to
succeed empty for that one; the refusal line and the named file are the evidence, never the code.

Still open, unchanged from Session 23: rubric rebalancing (S21), `cache.ts` `clearCache()` dead
code, Cogno review, CE-distributed credentials guard idea.

## Afternoon: learnings show WHEN, and `--since` answers "what was saved today"

Trigger: two agents this morning (the invoc merge session, and this one at its first read) probed
`learnings.json` with a key the records do not have (`createdAt`), got empty strings, and one of
them answered "0 saved today" with confidence. A probe on a missing key returns a confident zero,
never an error. The store held 22 records from the day, under `created`.

Done, LOCK `[LEARNINGS-LIST-SHOWS-CREATED]` in `src/learnings.ts`:
- every listed learning shows `Learned: 2026-09-05 10:30Z (12:30 CEST)`, UTC instant plus
  Europe/Zurich wall time; a record without a usable instant says `undated`, not blank.
- `contextengine list-learnings [cat] --since today|yesterday|ISO` and the MCP tool's `since`
  param, same renderer. `today` is the Zurich calendar day (midnight read from Intl, never a
  guessed offset; checked on both DST days and at the exact boundary). An unparseable spec is an
  error with exit 1, not a zero. An empty result names the boundary it applied and the real field.
- 14 tests in `src/learnings-format.test.ts` (pure functions), suite 493 green in 28 files.
- AGENT_RULES_CE.md § 6 and SKILLS.md carry the rule: read learnings through the tool, never the JSON.
- Local surfaces rebuilt, launchd MCP restarted. Claude Code's `npx` entries need a publish: ready
  for 2.5.5 on Yan's go, not published.

Decided against, with Yan: a session-doc timer every 30 minutes. A timer writes on schedule, not
on change, and costs a wakeup each time; the trigger that works is "significant change" plus the
mechanical backstops already in place (post-push `end_session`, git's UTC stamps).
