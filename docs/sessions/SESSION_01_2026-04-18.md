# SESSION_01 — 2026-04-18 — Hook-based compliance enforcement

## Goal
Force Claude (and subagents) to comply with CE rules — especially Rule 10 (post-commit `end_session`) — by injecting `<system-reminder>` blocks via Claude Code's `PostToolUse` hook mechanism, rather than re-hardening CLAUDE.md prose again.

## What was done

1. **Plan written** → [/Users/yan/.claude/plans/resume-with-a-possible-reactive-kurzweil.md](/Users/yan/.claude/plans/resume-with-a-possible-reactive-kurzweil.md). Recommends 2 project-scoped hooks; explicitly does NOT recommend blanket read-only CE hook (owner edits CE daily).
2. **Parallel agent already installed a global Read hook** at `~/.claude/settings.json` (backup: `~/.claude/settings.json.bak.20260418-133559`). Fires `<system-reminder>` on any `Read` under `~/.contextengine/` or `~/Projects/ContextEngine/`, labeled as read-only reference material. Friction tradeoff accepted — owner confirms inline when editing.
3. **Project-scoped hooks written** → [.claude/settings.json](../../.claude/settings.json). Two `PostToolUse` entries, jq-filtered:
   - **Hook A** (`matcher: Bash`): fires when `.tool_input.command` matches `\bgit\s+push\b`. Reminder orders the agent to call `end_session` MCP tool before ending the turn. Targets Rule 10.
   - **Hook B** (`matcher: Edit|Write`): fires when `.tool_input.file_path` ends in `ContextEngine/package.json`. Reminder to verify `files` array doesn't sweep in `server/`. Targets Rule 3.
4. **CLAUDE.md Rule 10 updated** — one-line pointer to `.claude/settings.json` so future agents recognize the injected reminders as legitimate CE hooks.
5. **Filter tests** (end-to-end, extracting exact command strings from the settings.json and piping fake tool-call JSON): all 6 cases pass. No false positives on `git pushx`, unrelated bash, FC_project's package.json, or other CE files.
6. **Feedback memory saved** → `~/.claude/projects/-Users-yan-Projects-ContextEngine/memory/feedback_compliance_enforcement_style.md` — pattern that user escalates by adding enforcement *below* the prose layer (hooks > timers > pre-commit > prose).

## What's live (verified after commit)

| Artifact | Path | State |
|---|---|---|
| Global Read hook | `~/.claude/settings.json` | Installed, **activates next session** |
| Project Bash+Edit hooks | `.claude/settings.json` | Committed + pushed, **activates next session** in CE |
| CLAUDE.md pointer | `CLAUDE.md` Rule 10 | Committed + pushed |
| Feedback memory | `memory/feedback_compliance_enforcement_style.md` | Active now |

**Hooks do NOT affect the current Claude Code session** — they load at Claude Code startup. Owner must exit Claude Code and relaunch in CE to activate.

## Commit hashes

- `f1688e4` — feat(hooks): PostToolUse <system-reminder> injection for Rule 10 + Rule 3

Pushed to `origin/main` (was `0b1646c..f1688e4`).

## LOCK blocks added

None. Hooks are inherently reversible (delete `.claude/settings.json`) and don't need the LOCK convention — their presence in the repo is self-documenting.

## What's pending

- **Next-session smoke test**: after owner relaunches Claude Code in CE, make a trivial edit, run `git push`, confirm the Hook A reminder appears in the Bash tool result. If no reminder → run `jq . .claude/settings.json` to confirm JSON validity.
- **Decide Tier 2 hooks** (deferred, still open):
  - LOCK-block preservation hook (global) — benefit real but needs a helper script, not a jq one-liner. Skip until Hook A proves the pattern.
  - `--no-verify` bypass hook — redundant with existing 3-layer defense from `f2337b9`.
- **51 items flagged by `end_session`** — all pre-existing drift in other projects (FC_project, KONIVE.com, admin-invoc-node, invoc-mobileapp, invoc.io, FASTPROD uncommitted; several projects stale docs). **Unrelated to this session.** Owner-driven triage when convenient.

## Open questions

- Is the jq regex `\bgit\s+push\b` robust against shell wrappers the owner might use (`gp`, aliases, `hub push`, `gh pr create` → `git push`)? Alias-based pushes wouldn't trigger. If that's a real workflow, the matcher needs broadening. **Flag to check** on next failure.
- Does the global Read hook create friction in editing sessions? Owner accepted theoretical friction; watch for real complaints over next few CE sessions.
- Should Hook A fire on `git commit` too, or only `git push`? Current choice: push only (commit without push is half-done state). Revisit if agents commit without pushing and still skip `end_session`.

## Unresolved bugs

None introduced this session.

## Future-me: read these first

When resuming CE compliance-enforcement work:

1. **[.claude/settings.json](../../.claude/settings.json)** — the current hooks. Canonical source for what fires when.
2. **[CLAUDE.md](../../CLAUDE.md) Rule 10** — explains *why* the post-push reminder exists and names the bias it counters.
3. **[/Users/yan/.claude/settings.json](/Users/yan/.claude/settings.json)** — global hooks (the Read hook lives here). Check before adding any new global hook — don't duplicate.
4. **[/Users/yan/.claude/plans/resume-with-a-possible-reactive-kurzweil.md](/Users/yan/.claude/plans/resume-with-a-possible-reactive-kurzweil.md)** — the plan that produced this session. Includes the jq escaping notes and the Tier 2 recommendations I didn't install.
5. **Recent compliance-enforcement commits** (pattern context):
   - `7a51d43` strengthen CE post-commit checkpoint wording
   - `7838218` 10-minute session save timer
   - `f2337b9` 3-layer --no-verify defense
   - `f1688e4` (this session) hooks
