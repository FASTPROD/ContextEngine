# Session 02 — 2026-04-18

**Scope**: Rule 10 enforcement hardening, CLI fallback path, v1.23.1 prep

## What was done

### 1. Anti-drift hooks committed earlier (from Session 01, `f1688e4`)
- `.claude/settings.json` — PostToolUse hooks injecting `<system-reminder>` after `git push` (Rule 10) and edits to `package.json` (Rule 3).
- Verified firing: session jsonl confirms `hook_success` attachment on real `git push` (not just the diagnostic test).

### 2. Hook-enforced rules trimmed to one-liners (`2bbe0e2`)
- `CLAUDE.md` Rules 3 and 10 — removed hardening prose ("DO NOT DEPRIORITIZE", "Agents have a known bias…"). One-liner policy + pointer to the hook.
- Rationale: hardening text was cargo-cult from past failures. Hook is the real enforcer; prose was load-bearing nowhere.

### 3. CLI fallback path for `end_session` (`4cc76bd`)
Four files touched:
- `.mcp.json` (new) — Claude Code MCP config at project root, npx-based for portability. Dogfoods own copilot-instructions.md Rule 12.
- `.claude/settings.json` — hook reminder now names both paths (MCP tool + `npx @compr/contextengine-mcp end-session`).
- `CLAUDE.md` Rule 10 — one-liner with both invocation paths.
- `.github/copilot-instructions.md` Rule 11 — trimmed from hardening prose to actionable one-liner + full protocol checklist.

### 4. v1.23.1 release prep (`e18ae4a`, **NOT YET PUBLISHED TO NPM**)
- `skills/contextengine/SKILL.md` — post-commit verification section rewritten to document both paths (MCP + CLI fallback). This file **does ship to npm** (paying users read it).
- `package.json` version: 1.23.0 → 1.23.1.
- `CHANGELOG.md` — 1.23.1 entry added.
- Build clean (`npm run build` passed, firewall obfuscation 25.7KB → 9.1KB).
- Smoke test passed: `npx . search "end_session"` returned results.

## What's live

| Change | Location | Status |
|---|---|---|
| Rule 10 / Rule 3 hooks | `.claude/settings.json` on `main` | ✅ Committed + pushed |
| `.mcp.json` (Claude Code) | Project root on `main` | ✅ Committed + pushed |
| CLAUDE.md one-liners | `main` | ✅ Committed + pushed |
| copilot-instructions.md Rule 11 trim | `main` | ✅ Committed + pushed |
| SKILL.md CLI fallback doc | `main` | ✅ Committed + pushed |
| **npm @compr/contextengine-mcp v1.23.1** | npmjs.com | ✅ **Published 2026-04-18 18:26 CEST** |

Paying users now see v1.23.1 with the updated SKILL.md documenting the CLI fallback path.

### npm auth change (2026-04-18)
- Old classic token `npm_F125...r58Q` (no 2FA bypass) blocked publish with E403.
- New **granular access token** `ContextEngine-Publish-GranularApr2026` created on npmjs.com — scoped to `@compr/contextengine-mcp` read+write only, bypasses 2FA automatically (granular tokens with single-package scope bypass 2FA by npm policy), expires Jul 17, 2026.
- Token stored in `.copilot-credentials.md` under `## npm Publishing`, backed up to Google Drive vault via `~/Projects/backup-credentials.sh`.
- `~/.npmrc` restored with new token, chmod 600.

## What's pending

### Token cleanup on npmjs (optional hygiene)
The following tokens are now superseded and can be deleted from npmjs.com → Access Tokens:
- `ContextEngine-Token-March2026` (`npm_F125...r58Q`) — classic, no 2FA bypass
- `ContextEngine-Publish-90days` (`npm_F0hM...SVc4`) — unused
- `ContextEngine-Publish` (`npm_gz8C...F8dR`) — unused

Already expired (safe to delete or ignore):
- `ContextEngine-Token-unscoped` — expired Mar 18
- `ContextEngine-Token` — expired Mar 18

Keep only `ContextEngine-Publish-GranularApr2026`.

### Pre-existing cross-project staleness (non-blocking)
`end_session` reports 50/100 — all failures in other projects (invocme-*, shop.invoc.io, legacy-php-reference missing or stale docs). Nothing caused by this session's changes.

## Open questions
- Should `.mcp.json` use local `dist/index.js` + absolute node path (Yan's fast dev loop) or stay on `npx` (portable for clones)? Currently `npx`. First startup will be slower but always fresh.
- `~/.npmrc` was missing at session start — did it get cleaned up by another process, or was it never created on this machine? Restored from `.copilot-credentials.md` during this session (chmod 600, 74 bytes).

## Commit hashes (chronological)

| Hash | Message |
|---|---|
| `f1688e4` | feat(hooks): PostToolUse <system-reminder> injection for Rule 10 + Rule 3 |
| `2bbe0e2` | docs(CE): trim hook-enforced rules to one-liners |
| `4cc76bd` | feat: CLI fallback path for end_session, dogfood .mcp.json |
| `e18ae4a` | release(v1.23.1): end_session CLI fallback in SKILL.md |

## LOCK blocks added
None this session. Changes were doc/config edits; no fragile fixes qualified.

## Unresolved bugs
None introduced. Pre-existing 50 cross-project staleness warnings remain — out of scope.

## Files future-me should read first
1. [CLAUDE.md](../../CLAUDE.md) — Rule 10 one-liner (line 28)
2. [.github/copilot-instructions.md](../../.github/copilot-instructions.md#L220) — Rule 11 trim (shipped-doc equivalent)
3. [skills/contextengine/SKILL.md](../../skills/contextengine/SKILL.md#L225) — "Post-Commit Verification" section (this is what paying users' agents read)
4. [.claude/settings.json](../../.claude/settings.json) — hook reminder text + jq matchers
5. [.mcp.json](../../.mcp.json) — Claude Code MCP config (npx form)
6. [CHANGELOG.md](../../CHANGELOG.md) — 1.23.1 entry
7. [.copilot-credentials.md](../../.copilot-credentials.md) — npm token section (source of truth for publish auth)

## Key architectural decision locked in

**The "hook + CLI fallback + one-liner rule" pattern** now applies to all CE compliance rules that get skipped:
1. Hook enforces mechanically (project Claude Code only).
2. Rule text in `CLAUDE.md` + `.github/copilot-instructions.md` + `SKILL.md` = **one line**, names both MCP + CLI paths.
3. CLI fallback via `npx @compr/contextengine-mcp <cmd>` works for all downstream agents (Cursor, Copilot, Claude Desktop, terminal).

Rule 13 (`save_learning`) already followed this pattern pre-session; Rule 10 (`end_session`) now matches. If another rule ever needs hardening prose, the prose should be deleted and the enforcement moved into a hook + CLI path.
