# Session 22 — 2026-08-19 to 2026-08-21 — the cost collector, two gates that were never called, four releases, and a server deployed after six months

> **This file spans three days and twelve sections.** The filename carries the start date
> only. Sections A to E are 2026-08-19; F to K are 2026-08-20 (2.5.0 to 2.5.3, the audit log
> rotation, the activation server deploy); L is 2026-08-21. If you are looking for "the
> session doc of the 20th or 21st", this is it.
>
> **Closed 2026-08-21.** Continues in [SESSION_23](SESSION_23_2026-08-21.md). Do not append
> new sections here; parallel sessions writing to one file produced two sections lettered K.


**Scope**: built `contextengine cost` from Claude Code's own transcripts, then audited every
CE guard rail by executing it. Two gates were enforcing nothing.

Continues from [SESSION_21](SESSION_21_2026-08-16.md).

## A. The format had a 2.8x trap in it

Instruction was "don't assume the format — inspect real files first." Doing so paid immediately.

One assistant `message.id` is written across **several JSONL lines** (one per content block:
thinking, text, each `tool_use`), and **every line repeats the same `usage` object**. Summing
per line reported **624,873 cache_read tokens where the true figure was 225,183** — 2.8x over,
and 4.6x on `cache_creation`.

Verified invariants over **1,609 message ids in 126 files** before relying on them:

| Invariant | Result |
|---|---|
| `input`/`cache_creation`/`cache_read` constant within a `message.id` | **0 exceptions** |
| `output_tokens` monotonically increasing within an id | **0 exceptions** — so max is the final count |
| one `agentId` per file | **0 files with more** |

Locked as `[TRANSCRIPT-DEDUP-BY-MESSAGE-ID]`. A cost report that overstates by 3x is worse than
none: it gets disbelieved once, then ignored forever.

**Second trap: terminal state is not the last line.** 2,090 of 2,310 transcripts end on a `user`
line — the tool_result for the agent's own final `StructuredOutput` call. Reading the last line
classified 2,146 healthy agents as "no report" and would have made `fanout_without_canary` fire
on every workflow ever run. Locked as `[AGENT-REPORTED-IS-NOT-LAST-LINE]`. Correct classification
across the corpus: **2,285 reported / 19 capacity_exhausted / 3 api_error / 2 no_report / 1 output_cap**.

## B. Cost, not tokens — and the two stories that disagree

Weighted cost is `input*5 + cache_write*6.25 + cache_read*0.5 + output*25` per million (Opus 5).
Rates live in `.contextengine/policy.json` → `agent_cost.pricing`, never hardcoded
(`[PRICING-LIVES-IN-POLICY]`). No `*` catch-all on purpose: an unknown model reports as
**UNPRICED**, never as $0.

The volume ratio and the cost ratio point in opposite directions, which is the whole point:

| Run | Output by volume | Cache reuse | Valued cost | Verdict |
|---|---|---|---|---|
| `wf_67cbce22` (30 agents) | 1.8% — *looks alarming* | 8.3x | $34.78 vs $120 uncached | **healthy** |
| `wf_f676d824` (722 agents) | 7.5% — *looks better* | **0.4x** | $209, **68% on cache WRITES** | **pathological** |

So `context_burn` fires on **cache inefficiency, tool-call inflation, and per-agent cost** — and
deliberately **never** on a low output/volume ratio (`[BURN-IS-COST-WEIGHTED-NOT-VOLUME]`). Firing
on the ratio would flag every well-cached run and burn the alert budget, the exact failure the
`[DRIFT-HEURISTICS]` LOCK exists to prevent.

`fanout_without_canary` is a **time ordering**, not an outcome (`[CANARY-IS-A-TIME-ORDERING]`):
it counts agents that started before the earliest sibling *finished*. A completed 300-agent run
has 300 reports and was still un-canaried.

## C. The bug that hid inside the failures it was measuring

First run showed `⚠ 2.2M tokens UNPRICED`. Chasing it found a real defect.

Claude Code writes client-side notices ("You're out of usage credits", "API Error: …") as
assistant messages with model `<synthetic>` and **all-zero usage**. They land **last**, and the
collector took each agent's model from its last assistant message — so **every capacity-killed
agent was tagged `<synthetic>`**, and since that model has no price, its entire real consumption
was dropped.

The cost of the failure was missing from the report whose job is to surface that failure.
`wf_41771d7b` read **$29.78; the true figure is $42.70 — a 43% understatement**, concentrated
precisely on the 15 agents that died. Fixed by attributing tokens **per message model**
(`[PRICE-PER-MESSAGE-MODEL-NOT-PER-AGENT]`), with a regression test.

This is session 21 §J's rule earning its keep: the reading that lowered risk was the wrong one.

## D. Billing nuance: the dollars are notional

Verified: `subscriptionType: max`, no `ANTHROPIC_API_KEY`. Nothing here is billed per token.
So `contextengine cost` prints **three panels, never one** — volume, valued cost (labelled
NOTIONAL), and intensity (agents, tool calls, duration, deaths) as the capacity proxy. On a
subscription the scarce resource is the usage window, and the capacity signal is directly
observable: dead agents end with *"You're out of usage credits."*

| Run | Agents | Died at window |
|---|---|---|
| `wf_41771d7b` | 51 | **15 (29%)** |
| `wf_c6f7cbdd` | 4 | **4 (100%)** |

## E. Gate audit — executed, not read

Every gate exercised in a sandbox repo; premium tools tested with `HOME` redirected.

| Gate | Actually called? | Does what it claims? |
|---|---|---|
| secret-scan | ✅ pre-commit | ✅ blocks planted key, passes clean |
| doc-coverage | ✅ pre-commit | ✅ blocks missing section, passes when supplied |
| rule-parity | ✅ pre-commit | ✅ blocks 1-of-3, passes when synced |
| **commit-message-required** | ❌ **`init` never generated a commit-msg hook** | ✅ logic correct on all 3 paths |
| audit-verify | ✅ CLI | ✅ tamper→exit 2, orphan→exit 2, fork→warn+exit 0 |
| activation gate | ✅ CLI (3) + MCP (4) | ✅ refuses without licence |
| **score canary** | ⚠️ **CLI only — 0 call sites in MCP `score_project`** | ✅ real verdict, no vacuous pass |

### E1. `commit_message_required` was enforcing nothing

The rule type, its CLI subcommand and its tests all existed and all passed. `contextengine init`
installed **pre-commit and post-commit only**. Proven by execution: in a fresh repo with a
`severity: block` rule on `server/deploy.sh`, committing that file with a non-compliant message
**succeeded**. It worked in *this* repo solely because a commit-msg hook had been installed by
hand — so the author's own machine was the one place the gap was invisible.

Fixed: `generateCommitMsgHook()` + install in `init` (`[COMMIT-MSG-HOOK-MUST-BE-INSTALLED]`).
It cannot live in pre-commit — git does not populate the message file until after pre-commit
returns. Re-proven: non-compliant commit now blocked, compliant commit and unrelated files pass.

### E2. The canary covered the human path, not the agent path

`[SCORE-CANARY]` was wired on the CLI only. MCP `score_project` — the path Claude Code scores
through — had **zero** call sites, so a drifting scorer would be caught when a human typed the
command and missed entirely when an agent called the tool. Same shape as session 21 §H2. Fixed
as `[SCORE-CANARY-COVERS-EVERY-SCORER]`.

### E3. Two of my own reads needed the second check

Both would have been false findings, both caught by re-running a different way:

- *"commit-message gate blocks valid messages"* — I passed `--message-file`; the CLI takes a
  **positional** arg. It fell back to an empty message and blocked correctly.
- *"`check_ports` is ungated on the CLI"* — there is no `check-ports` CLI command. Unknown
  commands fall through to starting the MCP server, which is what I measured.

Also noted: `audit-verify` "exit=0 on a FAILED chain" was a pipeline artifact (`$?` of `head`).
Measured directly it is **2**. Three shallow reads, three retractions — the same discipline
session 21 §J paid for.

## F. Surface refresh — five surfaces, three actions (2026-08-20)

`npm run build` updates the artifact; it does **not** update anything holding that artifact in
memory. Checked from observed state (`ps` + `launchctl`) rather than from the table, per S21 §L.

| Surface | Runs | State after this session |
|---|---|---|
| Terminal `contextengine` | `npm link` → this repo | ✅ current — resolves the rebuilt `dist/` per call |
| VS Code MCP server | `.vscode/mcp.json` → `dist/index.js` | ⏳ needs a **window reload** (long-lived process) |
| **launchd autostart MCP** | `com.opscontext.mcp.plist` → same `dist/index.js` | ✅ **restarted** — see below |
| Claude Code MCP (project `.mcp.json`) | `npx @compr/opscontext-mcp` (**published**) | ⛔ still 2.4.3 — needs `npm publish` |
| Claude Code MCP (global `settings.json`) | `npx @compr/opscontext-mcp` (**published**) | ⛔ still 2.4.3 — needs `npm publish` |
| VS Code extension | shells out to the CLI | ✅ inherits the link |

**The launchd agent was 3 days stale**, exactly the S21 §L failure repeating:

```
BEFORE  PID 60932  started Mon Aug 17 14:04:39     ← dist/ rebuilt Aug 19 23:09
AFTER   PID 39184  started Thu Aug 20 09:46:49     ← current build, has the MCP canary
```

It had been answering every request with 17 August logic — including `score_project`, which is
precisely the path §E2 just added the canary to. A stale long-lived process produces no error;
it just keeps being wrong quietly.

**The two `npx` entries are the ones a reload can never fix.** They run the *published* package,
so they stay on 2.4.3 until `npm publish` — meaning the `commit_message_required` fix (§E1) and
the MCP canary (§E2) do not reach any Claude Code session on this machine until then. Nothing has
been committed or published this session.

## G. 2.5.1 — the release verified its own headline feature at $0.00 (2026-08-20)

2.5.0 published and verified. Two things the verification got wrong, both caught by re-reading
it rather than by any failing check.

**G1. Right conclusion, wrong evidence.** The publish report cited "the commit-message-required
hook fired on d4cc46b" as proof of §E1. That proves nothing: this repo is precisely the one place
the hook already existed by hand, which is the whole finding. Re-tested properly, `init` from the
**published** 2.5.0 tarball in a fresh repo: `commit-msg` is generated, a non-compliant commit on
`server/deploy.sh` is blocked, a compliant one passes. The fix holds. It simply had no proof until
now. Verifying a guard on the one surface where it already worked is §H2 repeating.

**G2. A defect filed as "not a bug".** Run outside a repo carrying `agent_cost`, `cost` printed a
full VALUED COST table of `$0.00` per row, `total $0.00`, and `caching saved $0.00 (0%)` over
**1.08 billion real tokens** whose true reuse was 8x. The warning line underneath was honest; the
panel above it was a confident wrong answer, and "caching saved 0%" inverts the actual finding.

The token accounting was correct. The **presentation layer** turned "I have no rates" into a
number, which is Session 21's rule at the display layer: any plausible-looking value returned from
a branch meaning *"I could not determine this"* is the bug, however reasonable it looks.

Worse, it was the **shipped default**: `DEFAULT_COST_THRESHOLDS.pricing` was `[]`, so the headline
feature of the release valued nothing for every user without their own rate table.
`[PRICING-LIVES-IN-POLICY]` meant *correctable without a release*, not *shipped with none*.

Fixed in 2.5.1:

| Change | LOCK |
|---|---|
| `src/default-pricing.ts` ships rates for the Opus/Fable/Sonnet/Haiku families, still overridden by policy.json, dated so staleness is visible | `[DEFAULT-RATES-SHIP-WITH-THE-PACKAGE]` |
| An `agent_cost` block that omits `pricing` falls back to the shipped table instead of pricing nothing | same |
| With nothing priced the panel prints **UNPRICED** and names the models to add; no `$` figure, no "caching saved" line | `[NEVER-RENDER-AN-UNKNOWN-AS-A-NUMBER]` |
| Partly-priced reports keep the total but label it a floor, naming what is missing | same |

Measured on the same data that produced the $0.00 panel (`--days 30`, 1,081.8M tokens):
**$1,975.93** valued against $6,390.60 uncached, 69% saved. That was the number the release was hiding behind a zero.

9 new tests (426 total), including one that pins the exact regression: a zero total may never be
classified as priced while tokens are unpriced.

**Also noted:** the audit log is at **192 MB / 523,885 records**, plus 40,086 records in 12 hours.
Reads are O(1) since 2.4.3 so this is not urgent, but there is no rotation or archival strategy.

## H. 2.5.2, two absent verdicts, one on the bedrock (2026-08-20)

Both follow-ups closed here are the same shape as §G, at different layers: a branch meaning
*"I could not determine this"* returning a plausible action or value instead.

### H1. A typo started a server

`cli.ts` dispatched on a long if/else chain ending in `else { import("./index.js") }`, so **any**
unrecognised token started a stdio MCP server waiting on stdin. No error, no exit code, no
output, just a hang. `contextengine scor`, `contextengine --version` and a renamed subcommand all
behaved that way.

It had already cost a wrong finding: §E3 recorded *"`check_ports` is ungated on the CLI"*. There
is no `check-ports` command at all. What got measured was a server booting.

Fixed as `[UNKNOWN-COMMAND-MUST-NOT-START-A-SERVER]`: unknown token names itself, suggests the
nearest command, exits 1. The server starts only on a bare invocation or the explicit `serve`
alias, and every launcher on this machine uses the bare form. `--version` now exists.
`KNOWN_COMMANDS` in `src/cli-commands.ts` is the single source of truth, and a parity test fails
if it drifts from the literals the dispatcher actually handles.

### H2. Rotation had to move history, because deleting it is what the verifier reports

The log was at **198 MB / 544,143 records**, growing 80,000/day, with no rotation path. The
reason it never got one is worth recording: the obvious implementation destroys the product.
`mv audit.log audit.log.1` makes the first record of the new file an **orphan**, and
`[VERIFY-FORK-IS-NOT-TAMPER]` classifies an orphan as deleted history, a hard failure. Scripted
logrotate would have turned a healthy chain into a permanent TAMPERED verdict.

`[ROTATION-MUST-NOT-ORPHAN-THE-CHAIN]`: a prefix moves into an append-only segment under
`audit-archive/`, and canonical history is *segments in order, then the live log*. Verification
is unchanged because the chain is still continuous.

Three findings came only from measuring, not from designing:

| Finding | LOCK |
|---|---|
| **Age does not bound size.** At 80k records/day a 30-day window still left **390,445** records live, and even 3 days left 205,422. Date retention alone would have shipped a feature that rotates and changes nothing. Added a record ceiling; whichever rule archives more wins. | `[DATE-RETENTION-DOES-NOT-BOUND-SIZE]` |
| **`push(...records)` overflows the stack** at 494,152 records. Every one of the 15 unit tests passed, all with chains of a few thousand. Found on the first real run, where post-rotation verify failed with *Maximum call stack size exceeded*. | `[NO-SPREAD-OVER-A-SEGMENT]` |
| **Both head readers returned genesis on an unparseable last line**, so a truncated record made the *next append* manufacture an orphan. The writer would have produced the log's hardest failure by itself. | `[UNREADABLE-HEAD-IS-NOT-GENESIS]` |

Ordering is archive-then-truncate (`[ROTATE-ARCHIVE-BEFORE-TRUNCATE]`), so the only reachable
crash state is a duplicated prefix, de-duped at the seam, never a lost one. Rotation refuses to
run on a chain that does not verify, and appends an `audit.rotate` record about itself.

**Real run:**

| | Before | After |
|---|---|---|
| live log | 198 MB, 544,143 records | **17 MB, 50,001 records** |
| archived | none | 494,152 records in `audit-0001.jsonl` |
| `audit-verify` | 544,143 ok, 66 forks | **544,154 ok, 0 altered, 0 missing, still exactly 66 forks** |
| detector hot path (`scanRecentEvents`) | 2,386 ms / 693 MB | **121 ms** |

That last row is the part that was not about disk: the drift detectors read the log on every
tick for a 5-minute window, so the whole history was being parsed to answer a question about the
last few minutes.

**Test strength was checked, not assumed.** Disabling segment reads fails 5 of the rotation
tests, including the zero-orphans assertion. A suite that passes both with and without the
feature would have proved nothing, which is §G1's lesson applied before writing the claim.

## I. A gate that was documented but never wired, and a scanner that never runs (2026-08-20)

A parallel session reported four findings about the hook layer and asked which to fix first.
All four verified. One was understated, and that one is the actual defect.

| Claim | Verified how | Result |
|---|---|---|
| An undocumented 8-hour `learnings.json` condition blocks commits | read the installed hook | **True, and it exits 1**, not a warning. `WARNINGS` is shared with the doc-freshness checks, so a stale learnings file alone blocks the commit |
| The connector repo's pre-commit has no CE freshness gate | listed its hooks | True, its `.py` commits are not blocked by staleness |
| `scripts/lock_inventory.sh` is not called by any hook | `grep -rn lock_inventory .git/hooks/` | **True, nothing calls it.** The LOCK count is checking nothing today |
| The global `CLAUDE.md` claims it "feeds a pre-commit gate" | read the file | True, the file was wrong |

### I1. The understated one: the appended secret scanner is unreachable

Reported as "pasted in twice by an append". It is worse and also less dangerous than that, which
is why it needed measuring rather than accepting.

The connector's pre-commit is 251 lines with an **unconditional `exit 0` at line 130**, at top
level, not inside any conditional. Everything from line 132, the whole block headed
`# --- Secret Scanner (appended) ---`, is dead code that never executes. It is not a duplicate
run, it is an unreachable half.

Exposure today is small: comparing both pattern arrays, the live block is a superset of the dead
one for every token type (Stripe, Google, Groq, GitHub, GitLab, Slack, AWS, Square, SendGrid,
plus two literal VPS passwords the dead block lacks). Only one generic heuristic is missing, the
one matching a credential-looking assignment of eight or more characters.

**Writing that heuristic out cost a blocked commit**, which is worth recording. Quoting its regex
verbatim in this paragraph tripped **this** repo's live scanner, since ContextEngine's own hook
does carry that rule. The fix was to describe the pattern in words rather than reach for
`--no-verify`. A scanner that fires on a true positive it cannot know is documentation is working
exactly as designed, and bypassing it to write a note about scanners would have been the worst
possible precedent.

The durable risk is the labelling. The dead half is the one that says *"Patterns are maintained
in a single place, update here, redeploy everywhere."* So the next pattern update lands in the
half that never runs, in every repo that received this hook the same way, and the update reads
as deployed.

### I2. The correction that mattered was in the global instructions

`~/.claude/CLAUDE.md` told every agent on this machine that `lock_inventory.sh` feeds a
pre-commit gate, and warned that migrating emoji to ASCII would make the gate "pass while
checking nothing". The real situation inverts that warning: there is no gate to fool. An agent
reading that paragraph would have taken a precaution against the wrong failure, and felt covered.

Corrected in place, dated, with the one-line `grep` that settles it, plus the dead-code warning
above. That file is not version controlled, so there is nothing to commit for it.

### I3. What CE does and does not own here

`STALE_HOURS` appears nowhere in `src/` or `dist/`, so **CE no longer generates the wall-clock
freshness gate at all**. The hook carrying it, including the 8-hour learnings condition, is a
hand-installed legacy artifact surviving in the repos that received it. Today `init` installs
pre-commit, commit-msg and post-commit built from `generatePreCommitHook()`,
`generateCommitMsgHook()` and `generatePostCommitHook()`, which are policy-driven, not
wall-clock. The hook itself says so in its own failure banner.

Recorded because the natural reading of "the CE gate blocked me" is that current CE did it. It
did not, and chasing the current source for a rule that lives only in an old installed file is
exactly the kind of search that ends in a wrong conclusion.

**Not a mystery writer:** the same report flagged CE writing `audit.log` at 17:13 and learnings
at 16:43 and suspected a parallel session owned the tree. That was this session, rotating the log
and publishing 2.5.2.

### I4. The CE-side defect the connector audit exposed

Checking whether CE could produce the connector's failure found a different one it already does.

`init` neither overwrites nor appends: it skips an existing hook. That is the correct choice,
both alternatives are worse, and appending is precisely what left the connector with an
unreachable scanner. But it announced the skip as one grey `⏭ already exists` line inside a
column of green ticks. The user reads "init done" and believes the policy gates are live. In that
repo they were never installed and enforce nothing.

Same shape as §E1 through a different door. There, the rule existed and no hook called it. Here,
a hook exists and does not call the rule. Both leave a gate that is present in every document and
absent at runtime, and neither raises an error.

It is not hypothetical: `invocme-odoo-connector` has a hand-written pre-commit, so `init` skipped
it, and none of that repo's CE gates have ever run. That is the same fact the parallel session
reported as "the connector has no CE freshness gate", seen from the installer's side.

Fixed as `[SKIPPING-A-HOOK-MUST-NAME-WHAT-IS-UNENFORCED]`: on skip, CE reads the existing hook,
and if it does not invoke the CE CLI, names the gates left unenforced and prints the line that
wires them in. It stays quiet when the hook already calls CE, so the warning keeps its meaning.
5 tests, including one asserting the foreign hook is byte-for-byte untouched.

## J. The activation server has been undeployable since February (2026-08-20)

A parallel session reported that `deploy.sh` carried the literal placeholder `<VPS_PASSWORD>`,
fixed it by switching the Gandi box to key auth (`0ae3912`, `[GANDI_KEY_AUTH_ONLY]`), and asked
when that server was last actually deployed. Checking answered a bigger question than it asked.

**How the placeholder got there.** `deploy.sh` last changed on **2026-02-27**, in commit `c270dd5`
*"security: redact SSH password from docs and deploy script"*. The redaction was correct and the
script was never repaired afterwards, so the security fix silently disabled the deploy path. Six
months passed without anyone meeting the failure, which is itself the finding: nobody ran it.

**Last real deployment: 2026-06-11.** `dist/server.js` on the box is dated `2026-06-11 13:45`,
`dist/` `13:59`. Ten weeks. `api.compr.ch` resolves to `92.243.24.157`, the Gandi box, so this
script does target the live host (the global `CLAUDE.md` table lists the activation server under
the OVH box, which is wrong).

**Three commits touch `server/` since that deploy**, and none of them are live:

| Commit | Date | What is not deployed |
|---|---|---|
| `1602127` | 2026-06-25 | the shared-learnings **Tier B server endpoint** |
| `727f902` | 2026-06-24 | docs only |
| `b696be1` | 2026-08-14 | delta-module version pinning |

### J1. Tier B is advertised by the published CLI and 404s in production

`src/community-sync.ts` posts to `https://api.compr.ch/contextengine/community-rules/fetch`, and
the published help text sells it as *"Tier B = api.compr.ch Pro"*. In production that route
returns **404 for POST**, byte-identical to the 404 for a route that was never defined, which was
used as the control. A GET 404 alone would have proved nothing, since Express answers
`Cannot GET` for any unmatched method on a POST-only route: the verdict needed the POST and the
control to mean anything.

So a shipped, documented Pro feature has been pointing at a nonexistent endpoint since it was
written. The client treats network failure as a fall-back to the cached store, so it degrades
quietly rather than erroring.

### J2. The stale delta modules are a regeneration gap, not a deploy gap

The box serves `delta-modules/manifest.json` at **version 1.19.1, generated 2026-02-23**, against
a package now at 2.5.3. That looks like the exact failure `b696be1` was written to prevent. It is
not, for two reasons, both checked rather than assumed:

- The repo's own `server/delta-modules/manifest.json` is **also 1.19.1**. Deploying would change
  nothing; the modules need regenerating, which is a separate pipeline.
- Nothing consumes them. `loadDeltaModule` has **zero production call sites**, verified three
  ways: `grep` over `src/`, over `dist/`, and over the **published 2.5.3 tarball**. Only the
  definition, its `.d.ts` declaration and one test refer to it.

That second point lowers the risk, so it was the one to re-derive rather than accept, per §G. The
conclusion holds: the stale modules are inert today. They stop being inert the moment Option 3
wires the loader, which is precisely why `b696be1` exists and precisely why it should be deployed
before that wiring, not after.

**Healthy parts:** `/contextengine/health` and `/contextengine/pricing` both answer 200, the
service reports 3 delta modules, 1 active licence, 1 active activation, Stripe enabled.

## K. The first deploy in six months took four runs, and each failure was already in the script (2026-08-20)

§J found the activation server ten weeks behind. Deploying it was approved (no customers yet, so
the worst case was a few minutes of a broken API nobody calls), coordinated with the parallel
session remediating the same box, and run by Yan by hand because the permission classifier blocks
`./deploy.sh` from this session. Four runs. None of the failures were new; every one had been
sitting in `deploy.sh` since February, unmet because the `<VPS_PASSWORD>` placeholder made the
script unrunnable. A script nobody can run accumulates defects nobody can see.

| Run | Died at | Root cause | LOCK |
|---|---|---|---|
| dry-run | `deleting ecosystem.config.cjs` | the PM2 config lives only on the box; `--delete` would have removed it. `pm2 restart` works off the in-memory list, so the deploy would have reported success and the service would not have returned at the next reboot, which was scheduled for the same day | `[NEVER-DELETE-THE-PM2-ECOSYSTEM-FILE]` |
| 1 | `npm install` | `better-sqlite3` needs C++20; the box is Debian buster, `g++` rejects `-std=c++20`, no prebuilt binary for its glibc. Deps were byte-identical to the box, so the install was pure risk | `[NEVER-REINSTALL-NODE_MODULES-ON-THIS-BOX]` |
| 2 | `tsc` on the box | `node_modules` drifted since February: stripe 20.3.1 against a `^14.25.0` pin, `@types/nodemailer` absent. Unrepairable in place because of run 1 | `[COMPILE-LOCALLY-NEVER-ON-THIS-BOX]` |
| 3 | succeeded, wrong manifest | `gen-delta.js` falls back to the literal `1.0.0` when called without a version, which this script did. It overwrote `1.19.1` with a placeholder | `[STAMP-THE-DELTA-MANIFEST-WITH-THE-REAL-VERSION]` |
| 4 | succeeded | | |

Two of those deserve more than a table row.

**Run 2 left the box in a mixed state, and that was the dangerous one.** `tsconfig` has no
`noEmitOnError`, so the failing `tsc` still emitted a complete new `dist/`, including the Tier B
module that had never existed on the box, and only the `pm2 restart` was skipped. New code on
disk, seventy-day-old code in memory. The parallel session had a reboot scheduled for Task 2; it
would have swapped the versions with nobody having decided to. Checked the emitted artifact
before touching anything (`node --check`, route registered, module imports cleanly) and it was
sound, which is the only reason the state was merely wrong and not broken.

**My own fix had a bug in it.** The exclude added after the dry-run covered
`ecosystem.config.cjs` but not the `.bak` the script writes one line earlier, so run 1 logged
`deleting ecosystem.config.cjs.bak`: the safety copy was removed right after being taken. The
live file was never at risk; the safety net was decorative. Fixed to `ecosystem.config.cjs*`.
Found by reading the run output rather than the exit code, which is the habit §E3 cost.

**Verified live after run 4:** Tier B answers **400** with `Missing required fields:
license_token, machine_id` where it answered 404, which is the correct response to an
unauthenticated probe. `/health` and `/pricing` 200. Manifest **2.5.3**. `ecosystem.config.cjs`
intact with its backup present. `dist/server.js` dated today. PM2 shows the other four tenants on
the box untouched.

**Not fixed, recorded:** `gen-delta` now emits five modules (`agents`, `rubric`, `collectors`,
`search-adv`, `firewall`) while `server.ts` serves a hardcoded list of three. `rubric.js` and
`firewall.mjs` are generated and never served. Generator and server have drifted; `/health`
saying `deltaModules: 3` is the visible symptom.

## L. "Can I still work with you?" (2026-08-21)

Yan asked for an adversarial pass on the scanner fix before it went to 25 repos, saying he
expected more bugs. He was right: five, none visible from a green bench (commit `89a9f1e`).
Then he asked whether he could still work with me. That question belongs in the record more
than the bugs do.

**The honest split.** Defects found in pre-existing code that day (the `*.env` exemption, the
`+` line, the `<VPS_PASSWORD>` placeholder, bare `pm2`, `--production` breaking `tsc`, the dead
scanner, ten weeks of undeployed server) were all real and had sat for months. Defects I
introduced: `\x27` in patterns, `\s` inside a POSIX bracket, the `|` separator truncating
regexes, my own exclude deleting the `.bak` it had just written, a bench that did not mirror
the hook, `git add -A` sweeping a stray file, and `\x27` **reintroduced** after its fix. Seven.
All caught by my own tests before reaching any repo. The seventh is the one that matters: a
repeated error shows that working memory does not protect against recurrence. Only a
mechanical guard does, and I had not built one.

**Why it happens.** I generate fast and verify after. On shell regex with three levels of
quoting, two shells (zsh runs the hook, bash runs the bench) and a `grep -E` that ignores
`\xNN`, the first write is wrong more often than not. I did not know that well enough.

**What changed, written in three places so it outlives this chat:**

| Where | What | Read by |
|---|---|---|
| memory `feedback-adversarial-pass-before-deploy` | the two rules and the why | every session on this project |
| CE learning, category `testing`, "Agent self-knowledge" | same, portable | every agent on every project via `search` |
| `~/.claude/CLAUDE.md` § Adversarial Pass Before Deploy | short form, pointing to both | every agent, before writing |

The rules: any code that leaves its repo gets an attacking second pass before deploy, unasked;
every fix carries its LOCK in the same commit; a bench that fails on the forbidden shape beats
a rule to remember. The `\x27` bug now has `[NO-HEX-ESCAPES-IN-ANY-PATTERN]` and the bench
fails mechanically on any hex escape, proven by injecting one (`08a9d0e`).

**The gap this section closes.** When asked, I said "I will put this in memory" and did not,
until Yan asked whether it was done. Saying a rule is not writing it. That is the same
defect as §E1 and §I, in the agent instead of the code: present in every conversation,
absent at runtime.

## Verification

| Check | Result |
|---|---|
| `npm run build` | clean |
| `npx vitest run` | **463 tests pass, 24 files** (was 426/21 earlier in this session, 363/18 in S21) |
| New `tests/transcript-collector.test.ts` | 33 tests: dedup, per-model attribution, canary ordering, cost math, pricing status |
| Cost model vs published figure | 65.7M cr + 4.4M cw + 534k out → **$73.70** (~$74), **$363.85** uncached (~$364) ✅ |
| Real audit log | **483,799 → 544,154 records, 0 altered, 0 missing, still exactly 66 forks** (494,152 of them now in an archive segment, §H2) |
| New test files | `audit-rotation` 20, `cli-dispatch` 12, `init-hooks` 5, plus 9 added to `transcript-collector` |
| Published packages | **2.5.0**, **2.5.1**, **2.5.2**, **2.5.3**, each verified from the published tarball, never from the local build |
| Repos written by this session | **1**, this one. Every CLI verification ran in a sandbox with `CONTEXTENGINE_WORKSPACES` redirected, and `score` always with `--no-save`, confirmed by an unchanged `SCORE.md` mtime |
| Files written outside any repo | **2**, both deliberate: `~/.contextengine/audit.log` rotated for real (§H2), and `~/.claude/CLAUDE.md` corrected (§I2) |

**Correction to this table.** Until the 2.5.3 entry it read *"Repos written by this session: 0,
all verification sandboxed"*. That was written before §H2 rotated the real audit log and §I2
edited the global instructions file, and it stayed there while both happened. The sandboxing
claim was true and remains true for every CLI verification; the "0 written" claim was not, and a
verification table is the last place a stale reassurance belongs.

### One mistake worth keeping

`git add -A` in the §I4 commit swept up `ContextEngine-200826.code-workspace`, an untracked
editor file that had been sitting in `git status` since the session opened. It is now committed
and on both remotes. It does not reach npm, the `files` field does not include it, so the damage
is a stray file in the repo and nothing more. Recorded rather than quietly reverted: `add -A`
after a long working session commits whatever else drifted in, and the fix is to stage by path,
which every other commit today did.

## Follow-ups

- [x] ~~launchd MCP server stale~~ — restarted 2026-08-20, PID 60932 (Aug 17) → 39184 (§F)
- [x] ~~Commit + publish~~, committed `707fcc8`, published **`@compr/opscontext-mcp@2.5.0`**
      2026-08-20, tagged `v2.5.0` (origin + gdrive). Verified from the **published tarball** in a
      clean temp install, never from the local build: `cost` present in `help`, `cost --json`
      exit 0, `score <path> --no-save` wrote nothing, `audit-verify` exit 0 on 518,837 records,
      0 altered, 0 missing, still exactly **66 forks** (no new fork from the publish).
      Two behaviours worth recording, both correct by design:
      `prepublishOnly` leaves `dist/rubric.js` **obfuscated** behind it, so a plain `npm run build`
      is mandatory afterwards or local tooling silently runs the published artifact; and with no
      `agent_cost` in a project's `policy.json`, `cost` warned that tokens were unpriced.
      **That second one was recorded here as correct-by-design and was not.** It printed a full
      `$0.00` cost table *above* the warning, including `caching saved $0.00 (0%)`. See §G2; fixed
      in 2.5.1.
      launchd MCP restarted onto the rebuilt `dist/`: PID 45088 (10:01) → **54223 (10:44)**, build 10:43.
- [ ] **Reload the VS Code window**, the one action that refreshes both long-lived surfaces here:
      VS Code's own MCP server and, because Claude Code runs as the VS Code extension on this
      machine, its on-demand `npx` MCP children too. Publishing is necessary, not sufficient.
- [x] ~~2.5.1~~ — committed `512a21c`, published **`@compr/opscontext-mcp@2.5.1`** 2026-08-20,
      tagged `v2.5.1` (origin + gdrive). Verified from the **published tarball**: 11 shipped rates
      dated 2026-08-20, and data that previously priced at `$0.00` now values at **$1,395.87**
      (`--days 7`, 875.9M tokens; the §G2 figure is the wider `--days 30` window, so the two are
      not the same measurement). The
      UNPRICED path renders **no currency figure at all** (checked by regex over the block).
      `dist/rubric.js` restored, 1 obfuscation marker → 0. launchd PID 59773 (10:59:58) against a
      10:59:36 build.
- [x] ~~Unknown CLI commands silently start the MCP server~~, fixed in 2.5.2 as
      `[UNKNOWN-COMMAND-MUST-NOT-START-A-SERVER]` (§H1). Unknown token exits 1 with a
      suggestion; `--version` and a `serve` alias added; parity test guards the command list.
- [ ] `contextengine cost` is CLI-only; no MCP tool yet.
- [x] ~~Audit log growth: no rotation or archival path~~, fixed in 2.5.2 as
      `[ROTATION-MUST-NOT-ORPHAN-THE-CHAIN]` (§H2). Live log 198 MB to 17 MB, history intact
      and still verifying. **Not automated on purpose**: rotation is a manual command, since
      nothing should silently rewrite an evidence artifact on a timer.
- [x] ~~Publish 2.5.2~~, committed `3d23804` + `b66e24a`, published **`@compr/opscontext-mcp@2.5.2`**
      2026-08-20, tagged `v2.5.2` (origin + gdrive). Verified from the **published tarball** in a
      clean temp install: `--version` prints 2.5.2 (on 2.5.1 that same flag started a server and
      hung, so the flag answering at all is the fix demonstrating itself), `scor` exits **1** with
      a `score` suggestion, `audit-rotate` is in the help with `--max-records`, and `audit-verify`
      reads the archive segment: **551,750 records, 0 altered, 0 missing, still exactly 66 forks**.
      `dist/rubric.js` restored, 1 obfuscation marker to 0. launchd PID 91078 (16:00:12) against a
      16:00:00 build.
      **Surface note, measured rather than assumed**: no `npx`-spawned MCP server was running at
      all. Claude Code spawns them on demand, and `npx -y @compr/opscontext-mcp` already resolves
      2.5.2, so a fresh spawn is current without any cache clearing. The only long-lived process
      was the launchd agent. On this machine Claude Code IS the VS Code extension (one binary,
      up since 11 August), so a single **Reload Window** restarts it and its MCP children.
      Earlier entries here listed "reload VS Code" and "restart Claude Code" as two separate
      actions; on this machine they are one. A separate terminal `claude` would still need its
      own restart.
- [ ] Decide a rotation cadence. `audit-rotate` is manual today; at 80k records/day the live
      log crosses the 50k ceiling roughly every 15 hours.
- [ ] **Connector pre-commit: remove the unconditional `exit 0` at line 130** so the appended
      scanner block is reachable, or delete the dead half outright. Whichever is chosen, the
      "maintained in a single place" label must end up on the half that actually runs (§I1).
- [ ] **Decide whether `lock_inventory.sh` should be wired into a hook.** It is a script nothing
      calls. Either wire it, or stop describing it as a gate anywhere (§I2, global file already
      corrected).
- [x] ~~Publish 2.5.3~~, published **`@compr/opscontext-mcp@2.5.3`** 2026-08-20, tagged `v2.5.3`
      (origin + gdrive). Verified from the **published tarball** on the real failure case: `init`
      run in a fresh repo carrying a hand-written `exit 0` hook names the three unenforced gates,
      prints the wiring line, and leaves that hook byte-for-byte unchanged. `dist/rubric.js`
      restored, 1 marker to 0. launchd PID 32926 (19:05:38) against a 19:05:30 build. `npx` now
      resolves 2.5.3.
- [x] ~~Deploy the activation server~~, deployed 2026-08-20 22:20 after four runs (§K). Tier B
      live, manifest 2.5.3, all four other PM2 tenants untouched.
- [x] ~~Regenerate the delta modules~~, regenerated on the box at 2.5.3 by the deploy (§K).
- [ ] **`server.ts` serves 3 delta modules, `gen-delta` emits 5.** `rubric.js` and `firewall.mjs`
      are generated and never served. Reconcile the hardcoded list with the manifest (§K).
- [ ] **The box's `node_modules` is drifted and frozen.** stripe 20.3.1 vs `^14.25.0`, no
      `@types/nodemailer`, and it cannot be reinstalled there. Any future dependency change
      needs either a newer toolchain on the box or a vendored prebuilt `better-sqlite3`.
- [ ] **Correct the global `CLAUDE.md` VPS table**: it lists the activation server under the OVH
      box; `api.compr.ch` resolves to the Gandi box.
- [ ] Carried from S21: `catch { return "" }` audit; rubric rebalance; npm token rotation due 2026-09-09.
