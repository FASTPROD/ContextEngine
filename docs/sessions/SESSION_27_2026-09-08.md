# Session 27, 2026-09-08: LinkedIn review, 2.8.2 source summaries, the PWAs go mobile-only

Input: nineteen screenshots from Yan's LinkedIn and Instagram feeds (Claude Code skills, RAG
methods, agent context repos, safety-stock posts). Asked: what is worth doing, what to start.
CE session `contextengine-2026-09-08-linkedin-review` holds the running notes (4 keys).

## 1. Triage of the feed, decisions

- IPv6 to IPv4: the 2026-08-31 XBL incident, closed. Both VPSes re-verified read-only
  (gai.conf 5 precedence lines, LOCK marker, gandi resolves to 217.70.178.9). Gap fixed:
  CE learning `mtsimvft8ajv` now carries the fleet rule with a pointer to
  `admin.CROWLR/docs/sessions/SESSION_2026-08-31_SMTP_XBL_IPV6.md`.
- Mistral agentic search (Julien Lucas): the useful part for CE is "pick once, do not open three
  files". Shipped as source summaries, section 2. `read_source` navigation dropped: 0 calls in
  every transcript on this machine, 5 for `search_context`.
- Headroom, OpenViking comparison page: dropped by Yan. Graphify: only on a real refactor of
  FC_project, not on this repo. Impeccable: installed, run, disabled (section 3).
- Safety stock posts versus COMPR and invoc.me: COMPR already detects the distribution and
  uses a Gamma-Poisson conjugate for sparse items; invoc.me sets the service level per ABCD
  class with the cascade warehouse > product > vendor > default (I had wrongly read the engine
  default as "flat 95%", Yan corrected). Three small items in `COMPR/docs/BACKLOG.md`.

## 2. 2.8.2: every source says what it is

`src/source-summary.ts` (commit `989610d`): one line per source in `list_sources` and
`contextengine list-sources`, read from the first 4 KB only: frontmatter `description`, else
H1 plus first prose line (header metadata lines such as "Updated:" skipped), else the module
docstring or header comment for code. 824 of 828 configured sources get a line. 12 tests.
Release `a661c8c`, tag `v2.8.2`, `npm publish`, `verify-release` green both ways, marker
`~/.contextengine/verified-2.8.2`, CI + CodeQL + Doc Freshness success on both commits.

## 3. Impeccable, measured and disabled

Plugin 4.2.2 at user scope. Its detector found 0 issues on the compr.app hub screen and 4 in
the whole app, all animation nitpicks, while a plain read of the same 101 lines found
hard-coded colours that defeat the white-label brand and a hub listing two of four services.
Disabled, kept installed. The audit checklist is fine to apply by hand.

## 4. compr.app and app.crowlr.com: mobile-only gate, laptop landing, hub fixes

Yan's GO for A (hub lists all four services), C (mobile-only), D (theme colours), plus the
app.crowlr.com hub made reachable. COMPR-app `902b855`, app.CROWLR `17563c5`, both deployed
with their `deploy_frontend.sh --really-deploy`, live bundles checked for the new strings.
Shared files, identical in both repos: `src/utility/desktopGate.js` and
`src/components/common/DesktopLanding.jsx` (QR via `qrcode.react`, install steps, features,
no store badges). Tester flag `?desktop=1`. Not enforced server-side; the nginx hard block is
backlog item 1 in `COMPR-app/docs/BACKLOG.md` (Yan: no desktop scraping, mobile first; a
written plan first, it is a shared-VPS nginx change).

Two hook traps, both now CE learnings: doc-freshness wants copilot-instructions.md, SKILLS.md
and SCORE.md in the same commit as code (`contextengine score` rewrites SCORE.md); the secret
scanner's bare-value password rule matches the word bypass followed by a colon and a value, and prints no location.
