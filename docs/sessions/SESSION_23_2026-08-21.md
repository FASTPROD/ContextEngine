# Session 23 — 2026-08-21 — syncing the scanner to 25 repos, and what is still open

Continues from [SESSION_22](SESSION_22_2026-08-19.md), closed at 601 lines over three days.

## State at open

| Item | State |
|---|---|
| npm `@compr/opscontext-mcp` | **2.5.3**, tagged, verified from the published tarball |
| Activation server `api.compr.ch` | deployed 2026-08-20 after ten weeks; Tier B answers 400 not 404; delta manifest 2.5.3 |
| Audit log | 544k records, 494k archived in `audit-archive/audit-0001.jsonl`, live log 17 MB, chain intact, still exactly 66 forks |
| `hooks/pre-commit` | value-independent password patterns, 15 must-block / 42 must-pass fixtures, bench green under zsh **and** bash, 0 false positives on 45 real commits |
| `scripts/sync-hooks.sh` | written, both paths proven on a throwaway repo, **not yet applied** |
| Tests | 463, 24 files at open; 469, 25 files after the delta fix |

## Rules that govern this session, set at the end of 22

- Any code that leaves this repo gets an adversarial second pass before deploy, unasked.
  Memory `feedback-adversarial-pass-before-deploy`, CE learning "Agent self-knowledge",
  global `CLAUDE.md` § Adversarial Pass Before Deploy.
- Every fix carries its LOCK in the same commit. A mechanical guard beats a rule to remember.
- Shell regex benches run under both shells and must agree.

## Open, carried from 22

- [x] ~~`sync-hooks.sh --apply` on the 25 repos~~, done 2026-08-21. **25 replaced, 0 failed**,
      each proven by a planted secret the new hook refused. Independent check: an md5 of
      `git status --short` per repo, taken before and after, is identical on all 26, so no
      project file or index entry moved. 25 dated `.bak` files beside the hooks. `--check`
      afterwards: `current=25 drifted=0`.
      **The first real run crashed before its first iteration**: `KNOWN_LOCAL_LOGIC` had become
      empty when PLANK's gate was ported, and bash 3.2 under `set -u` treats an empty array
      expansion as unbound. My `--apply` test predated that change and I had not rerun it, one
      commit after writing down the rule that says to. Nothing was written; fixed (`7f5adb9`),
      retested on the throwaway repo, then applied.
- [x] ~~Connector pre-commit dead scanner~~, resolved by the replacement: the only `exit 0` in
      invocme-odoo-connector's hook is now its last line (382 of 382). Same for fc-hf-docker.
- [ ] `lock_inventory.sh`: wire into a hook or stop calling it a gate.
- [x] ~~Delta modules: gen-delta emits 5, `server.ts` serves a hardcoded 3~~, fixed 2026-08-21.
      `server.ts` now reads `manifest.json#moduleFiles` written by gen-delta (`server/src/delta-files.ts`,
      LOCK `DELTA-MANIFEST-IS-THE-LIST`, 6 unit tests, legacy three as fallback when the manifest
      is absent or corrupt). Proven on the real path: `gen-delta 2.5.3` against `dist/`, then the
      server loader returns the 5 (`agents`, `rubric`, `collectors`, `search-adv`, `firewall`).
      gen-delta also named the module `rubric.js` in `manifest.modules` because it stripped only
      `.mjs`; same helper now on both sides. **Not deployed**: the Gandi box has another terminal
      on it; `server/deploy.sh` on Yan's go, which also runs gen-delta remotely.

      **Finding that changes the weight of this item, checked twice (src and dist):
      `loadDeltaModule()` in `src/activation.ts` has no caller.** Correction an hour later: this
      was already written in `SKILLS.md` § Activation on 2026-08-14 ("Load path is still
      unproven"). Known and documented, not read. Same shape as the `lock_inventory.sh` gate
      claim: a fact that lives in a file the agent does not open is not a fact it has. `index.ts` and `cli.ts` import
      `agents.js`, `search.js`, `firewall.js` statically from the package. The delta bundle is
      fetched, decrypted, cached at `~/.contextengine/delta/` and never imported, so serving 3 or 5
      changes nothing a user sees today. This matches CLAUDE.md rule 3 ("no decrypt-at-runtime
      machinery exists in dist/"). Two consequences to decide on, not fixed here because both touch
      the published client: (a) the client writes every module as `<name>.mjs`, so `rubric.js`
      lands as `rubric.mjs` and `agents.mjs`'s `import "./rubric.js"` would not resolve if anyone
      ever did load it; (b) either wire `loadDeltaModule` in for the premium tools, or retire the
      delta mechanism and keep the activation gate as the sole moat, which is what rule 3 already
      says it is.

      **Decision, Yan, 2026-08-21: retire the delta.** No paying customer today, he is the only
      (enterprise) licence, so backlog priority, not a hotfix. Done client-side the same day:
      `installDelta`, `isDeltaInstalled`, `installedDeltaVersion`, `loadDeltaModule`,
      `PREMIUM_MODULES`, the `delta` response type and the `createDecipheriv` import removed from
      `src/activation.ts`; `gateCheck` no longer refuses on a missing cache; `deactivate` still
      empties a legacy `~/.contextengine/delta/`. LOCK `[DELTA-RETIRED]` replaces
      `[DELTA-VERSION-PIN]` (its subject no longer exists; the WHY is carried into the new block).
      Tests: 467 (two delta tests became two absence tests). Adversarial check on the build: fresh
      `HOME` without licence refuses `score_project`; real `HOME` with the enterprise licence and
      the June 1.19.1 cache passes, same as before. `deltaVersion` stays in the licence because it
      is part of the Ed25519-signed payload; the wire format did not move, so a 2.5.3 server and
      a 2.5.4 client agree. **Not published**: waits for the next release; server stops emitting
      `delta` once the new Gandi box is installed (Yan is reinstalling it, migration paused).
- [ ] `contextengine cost` has no MCP tool.
- [ ] Rotation cadence for the audit log: manual today, crosses 50k live records in ~15h.
- [ ] Global `CLAUDE.md` VPS table lists the activation server under OVH; it is on Gandi.
- [ ] Carried from S21: `catch { return "" }` audit; rubric rebalance; npm token rotation due
      2026-09-09, `prepublishOnly` warning starts 2026-08-26.

## Work log, 2026-08-21 afternoon

| Commit | What |
|---|---|
| `5c4a96c` | server: delta file list read from gen-delta's `manifest.json` (`server/src/delta-files.ts`, LOCK `[DELTA-MANIFEST-IS-THE-LIST]`, 6 tests). Not deployed. |
| `d069da7` | client: delta bundle retired (LOCK `[DELTA-RETIRED]`), SKILLS.md / README / CLAUDE.md rule 3 aligned, Cogno added to backlog. Not published. |

Both passed the scanner first time. Post-push `end-session` run after each.

### Environment notes
- **Gandi is being reinstalled** (Yan, this afternoon). All deploys to `92.243.24.157` paused
  until he says otherwise. `server/deploy.sh` must be repointed (IP, key, `ignoreip`) before the
  first deploy to the new box; the CLAUDE.md global VPS table (open item 5) gets fixed at the
  same time, with the new address.
- Commercial state, stated by Yan: **no paying customer today**, he holds the only licence
  (enterprise, super admin). Activation-side changes are backlog priority, not hotfixes.

### State at this point
| Item | State |
|---|---|
| Repo | `d069da7`, clean, pushed to origin and gdrive |
| npm | still 2.5.3; 2.5.4 pending, carries the delta retirement, to ship before the token rotation of 2026-09-09 |
| Tests | 467, 25 files |
| Local tooling | `npm run build` done, launchd MCP (`com.opscontext.mcp`) still on the 19:05 Aug 20 build; restart with `launchctl kickstart -k gui/$(id -u)/com.opscontext.mcp` when convenient |
| Server | code ready, not deployed; still emits the legacy `delta` field, to be removed with the new box |

### Still open, in suggested order
1. `contextengine cost` has no MCP tool (next).
2. `lock_inventory.sh` of the Odoo connector: wire into a hook or stop calling it a gate.
3. Audit log rotation cadence (manual, 50k live records in ~15h).
4. Global CLAUDE.md VPS table: activation server is on Gandi, not OVH; do it with the new IP.
5. From S21: `catch { return "" }` audit; rubric rebalance; npm token rotation 2026-09-09, warning from 2026-08-26.
6. Server: stop emitting `delta` on `/activate`, drop `delta-files.ts` and gen-delta's server step, once the new Gandi box exists.
7. Publish 2.5.4.

## Backlog, for review (added by Yan, 2026-08-21)

- **Cogno (cogno.studio)**, inbound pitch, to review for CE improvement ideas. Their framing: the
  layer above the coding agent. Not a scheduler (cron, hook, rule written months ago); watches
  Slack, GitHub and meetings, decides on its own when something is worth acting on and, their
  emphasis, which tasks *not* to do, then hands the work to the agent and a human reviews the PR.
  Written in Lisp so it rewrites its own workflow while running; their claim is that this is why
  no junk tasks and no nagging. "This space is crowded, so we intend to win it." 1-minute recording:
  https://www.loom.com/share/f82ee218ecad4108b19115fd5afbb9a3
  Questions to ask when reviewing: what in CE's firewall / learnings / audit log could play the
  "decide what not to do" role; whether a signal-driven trigger belongs in CE or stays out of
  scope (rule: no hosted/SaaS, BSL-1.1).
