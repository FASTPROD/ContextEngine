#!/bin/zsh
echo "🚀 Auto-pushing to remotes..."

BRANCH=$(git rev-parse --abbrev-ref HEAD)
git push origin "$BRANCH" && echo "✅ origin (GitHub)"
git push gdrive "$BRANCH" && echo "✅ gdrive (Drive backup)"

echo "📦 Done!"
